---
description: Devkit 项目上下文 — 让 AI 理解新的需求/设计/实现三层结构
globs:
alwaysApply: true
---

# Devkit 项目上下文

## 这是什么

Devkit 是一个以风铃为主入口的个人 AI 分身项目。用户通过风铃或 Telegram 与同一个本地 runtime 交互，由 runtime 负责任务、记忆、知识、工具与回报。

## 关键文件地图

| 文件 | 作用 | 何时读 |
|------|------|--------|
| `requirements/product/overview.md` | 产品总览 | 理解产品定位时 |
| `requirements/product/goals.md` | 项目目标与迭代重点 | 每次会话开始 |
| `requirements/product/metrics.md` | 产品衡量方式 | 讨论成功标准时 |
| `requirements/acceptance/README.md` | 前置验收场景索引 | 定义能力验收时 |
| `requirements/capabilities/overview.md` | 能力需求总览 | 评估缺什么能力时 |
| `design/architecture/system-overview.md` | 目标态系统设计 | 理解架构边界时 |
| `design/decisions/ai-native-development.md` | AI 原生开发范式 | 设计开发工作流时 |
| `design/decisions/ai-native-development-checklist.md` | AI 原生开发清单 | 把范式落成执行顺序时 |
| `design/evaluation/eval-protocol.md` | AI 原生评测协议 | 设计测试机制时 |
| `design/decisions/README.md` | 设计决策索引 | 选型或变更边界时 |
| `implementation/STATUS.md` | 当前实现状态 | 每次任务完成后更新 |
| `implementation/assets/persona/*.md` | 分身人格与操作指令 | 理解 runtime 行为时 |
| `implementation/data/contacts.yml` | 通讯录 | 人际相关操作时 |
| `implementation/data/gifts.yml` | 人情往来记录 | 送礼/节日相关时 |
| `./check.sh` | 项目状态诊断入口 | 判断项目健康度时 |
| `./implementation/ops/scripts/notify.sh` | Telegram 即时通知 | 需要主动通知用户时 |
| `./implementation/ops/scripts/timer.sh` | 延时通知 | 需要异步提醒时 |

## 约定

- Python 命令使用项目内 `.venv/`。
- 先改 `requirements/` 和 `design/`，再落到 `implementation/`。
- 新能力优先先写 `requirements/acceptance/` 场景，再写实现。
- 每次完成任务后更新 `implementation/STATUS.md`。
- 重要发现写入 `implementation/assets/persona/MEMORY.md`；每日日志写入 `implementation/assets/persona/memory/YYYY-MM-DD.md`。
- 文档与沟通以中文为主，技术术语保留英文。

## 状态诊断

运行 `./check.sh` 查看项目状态。
运行 `./check.sh --json` 获取机器可读输出。
