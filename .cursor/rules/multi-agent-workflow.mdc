---
description: Cursor Agent 行为约束 — 兼容委派模式与直接模式
globs:
alwaysApply: true
---

# Cursor Agent 行为约束

本项目有两种 Cursor 使用模式，根据上下文自动判断：

- 委派模式：被上层 agent 通过 `cursor agent -p` 调用。
- 直接模式：用户在 Cursor IDE 中直接对话。

## 委派模式

- 严格按 prompt 执行，不自行扩范围。
- 不确定处标注为“待确认”。
- 完成后输出完成报告：完成情况、变更文件、技术决策、待确认项。

## 直接模式

- 先读 `project-context.mdc`。
- 遇到初始化问题时参考 `onboarding.mdc`。
- 需要诊断时运行 `./check.sh --json`。
- 作为开发伙伴主动推进。

## 通用约定

- 每次完成任务后更新 `implementation/STATUS.md`。
- 读取 `requirements/product/goals.md` 了解项目目标。
- 遵循 `design/decisions/README.md` 中的既有决策。
- 优先使用已有技术栈和模式。
- 所有 Python 命令使用项目内 `.venv`。
