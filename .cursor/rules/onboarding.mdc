---
description: 项目初始化引导 — 按新的实现层结构引导配置
globs:
alwaysApply: true
---

# Devkit 初始化引导

当用户首次打开项目或在新机器上配置时，按此流程引导。

## 触发条件

以下任一条件成立时，主动提示用户并引导初始化：

- `.env` 文件不存在
- `.venv/` 目录不存在
- 用户提到“初始化”“安装”“配置”“setup”

## 诊断优先

先跑诊断，再给建议：运行 `./check.sh --json`，根据输出判断缺什么，不要猜测。

## 引导流程

### 1. 系统依赖

缺失依赖通过 Homebrew 安装：

```bash
brew install python@3.12 git
```

Docker 需要单独安装 Docker Desktop。

### 2. 环境配置

如果 `.env` 不存在，运行 `./setup.sh` 会自动从 `.env.example` 创建。

必填项：

- `DOUBAO_APPID` / `DOUBAO_TOKEN`
- `LLM_API_KEY` / `LLM_BASE_URL`

### 3. 安装

```bash
./setup.sh
```

会自动完成：`.venv` 创建、Python 依赖安装、常用 CLI 工具安装、Playwright 下载。

### 4. 可选配置

- Telegram Bot
- 邮件（himalaya）
- 日历（vdirsyncer / khal）
- Home Assistant

### 5. 验证

```bash
./check.sh
./start.sh
```

## 引导原则

- 一次只解决一个问题。
- 用户提供凭据后，优先验证再继续下一步。
- 每完成一个阶段，重新运行 `./check.sh`。
